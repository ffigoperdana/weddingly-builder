# Use this with docker-compose.db.local.yml for local PostgreSQL.
# The application runs on the host, so it connects through localhost:5433.
DATABASE_URL="postgresql://postgres:postgres@localhost:5433/weddingly"

POSTGRES_USER="postgres"
POSTGRES_PASSWORD="postgres"
POSTGRES_DB="weddingly"
POSTGRES_PORT="5433"
